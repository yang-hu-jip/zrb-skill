# 试点复盘与效率优化方案（v0.3）

> 试点样本：【京东视频助力】任务全链路（失败于外部活动页锁定，但链路能力全部打通）

---

## 一、问题清单（按影响排序）

### P0 — 卡住流程的问题

| # | 问题 | 现象 | 根因 |
|---|---|---|---|
| 1 | **二维码识别链路脆弱** | App 内置识别失败 → 长按菜单只给「浏览器/微信扫一扫」→ 浏览器识别失败 → 微信识别成功但被"诱导页"拦截 | App 二维码进的是低分辨率截图，非原图 |
| 2 | **外部活动页失效不可控** | 京东活动页「活动太火爆了」（活动已关闭） | 老单（9.8 发布）的活动已结束，报名前无预检 |
| 3 | **第三方 App 必须登录** | 京东 App 首次启动要登录，需短信/一键登录 | 助力类任务天然依赖用户真实账号 |
| 4 | **页面状态多变，定位失效** | 「立即报名」找不到；报名后变「重新报名」；报名前后详情页结构完全不同（预览版 vs 正式步骤版） | 没做"先识别状态再操作" |

### P1 — 拖慢效率的问题

| # | 问题 | 现象 | 根因 |
|---|---|---|---|
| 5 | **PowerShell 转义地狱** | `python -c "...中文引号..."` 和 `adb shell "复杂命令行"` 反复被吞、报 XPath 错误 | Windows PowerShell 参数解析把内层引号吃掉 |
| 6 | **固定 sleep 拖时间** | 每步 `time.sleep(4~10)`，全流程空等累计 >2 分钟 | 用"盲等"代替"条件轮询" |
| 7 | **探索成本重复支付** | 每步都全量 dump 控件树、反复滚动找元素 | 没沉淀"页面元素定位表" |
| 8 | **弹窗打断流程** | 照片权限弹窗、隐私政策、暗黑模式询问、跳转确认…每类都要临时处理 | 没预授权、没通用弹窗处理器 |
| 9 | **文件定位靠猜** | `find -mmin` 在 Android toybox 无效；相册路径找了 5 轮才定位到 `zrbdownload_*.png` | 没用 MediaStore 正确查询；不熟悉 App 落盘命名规律 |
| 10 | **微信弹窗无控件树** | 「扫一扫」菜单文本节点读不到，只能截图+坐标点 | 微信自绘控件不进无障碍树 |
| 11 | **中途人工介入无断点感知** | 你手动点了「重新报名」、手动登录京东后，脚本不知道状态变了 | 没有操作前状态检测机制 |

---

## 二、耗时分段实测

| 阶段 | 实测耗时 | 可压缩到 | 优化手段 |
|---|---|---|---|
| 大厅扫描 25 个任务 | ~8s | 3s | 控件树解析（已完成✅） |
| 详情页结构解析 | ~15s | 2s | 定位表缓存 |
| 长按→微信→识别失败→浏览器失败 | ~90s | 0s | 直接走原图落盘方案 |
| 原图定位（find 反复失败） | ~120s | 3s | MediaStore 查询 + 命名前缀匹配 |
| 原图本地解码 | 秒级 | 秒级 | ✅ 已跑通 |
| 跳转京东+活动页 | ~60s | 视外部 | 预检可提前判定 |
| 商店自动装京东 | ~40s | 40s | 一次性成本 |
| **合计（可优化部分）** | **~5.5 分钟** | **<1 分钟** | 见第三节 |

---

## 三、优化方案（落进 skill 的硬规则）

### 🚀 提速改造（对应你的"效率优先"）

1. **单入口 CLI**：`zrb.py <cmd>`（status/scan/apply/exec/upload/submit），每步先**检测状态**再动作，可断点续跑
2. **消灭盲等**：所有 `sleep` 换成 `wait_for(条件, 超时)` 轮询
3. **页面定位表**（`ui_map.yaml`）：探索一次沉淀一次，后续零 dump
4. **预授权一键脚本**：`pm grant` + `appops` 一次给全（众人帮/浏览器/相册/微信），消灭权限弹窗
5. **通用弹窗处理器**：每次操作前扫一遍已知弹窗关键词（同意/允许/知道了/暂不更新）自动点掉
6. **QR 任务极速通道**（本次最大收获）：
   ```
   长按二维码 → 微信扫一扫 → 知道了
   ↓（触发 App 把原图落盘 /sdcard/Pictures/zrbdownload_*.png）
   MediaStore 查最新 zrbdownload_* → adb pull → 本地解码（秒出 URL）
   ```
   ⚠️ 不再走微信 UI 取 URL（省 60 秒 + 免被诱导页拦截）
7. **报名前预检**：解码 URL → HTTP 探测活动页是否存活（关键字：活动太火爆/已结束）→ 不存活直接跳过，**不浪费报名名额**
8. **文本优先**：能读控件树就不截图；截图只用于"取证"
9. **脚本化铁律**：所有操作写 `.py` 文件跑，禁止 `python -c`/`adb shell` 传中文长参数（Windows 转义）

### 🛡️ 稳定性改造

10. **状态机**：`未报名 / 已报名待执行 / 已执行待提交 / 已提交待审核 / 已放弃`，每步先识别当前态
11. **人工闸门清单**（自动暂停并通知）：登录、验证码、实名、支付、人脸、风控页
12. **本地台账**（`journal.jsonl`）：任务/状态/证据路径/时间，支持"今天还有哪些待提交"
13. **反封控**：操作间隔 0.3~1.2s 随机抖动（在速度与安全间平衡），每日上限、金额上限保留

---

## 四、下次试点的标准流程（提速版，目标 <60s/单）

```
1. zrb.py scan            # 3s 扫大厅, 匹配模板/关键词
2. zrb.py inspect <任务>   # 读详情+要求, 红线检查, 输出执行清单
3. zrb.py apply <任务>     # 报名 (预检通过才报)
4. zrb.py exec <任务>      # 执行: 开链接/看视频/截图  (QR 走极速通道)
5. zrb.py upload          # 证据截图入库+选中上传
6. zrb.py submit          # 生成反馈文案 → ✋你确认 → 提交
```

**已知未验证环节**：截图「上传」控件的交互（选图器操作）——下次试点补上。

---

## 五、待办清单

- [ ] 写 `zrb.py` 骨架（CLI + 状态机 + 弹窗处理 + 日志）
- [ ] 沉淀 `ui_map.yaml`（大厅/详情/提交页元素定位）
- [ ] 预授权脚本（一次执行永久生效）
- [ ] QR 极速通道函数 `qr_fastpath()`
- [ ] 活动页预检 `precheck_url()`
- [ ] 补测「上传截图」流程
- [ ] 放弃当前卡住的京东单 + 换新单试点
