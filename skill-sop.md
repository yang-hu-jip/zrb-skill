# "众人帮" Skill SOP（工作流程规范）v0.3

> v0.3 变更：新增「铁律」章节（脚本文件执行、人工闸门）、提速改造（消灭盲等/定位表缓存/QR 极速通道/报名前预检）、状态机与台账

---

## 零、铁律（不可违背，优先级最高）

### 铁律 1：所有操作必须写成 .py 脚本文件执行
- **禁止** `python -c "...中文..."` 或 `adb shell "复杂命令行"` 直接传参
- **原因**：Windows PowerShell 会吞掉内层引号/转义（本次试点踩坑十余次，表现为 XPath 报错、命令截断）
- **执行方式**：
  - 每个操作写成独立 `.py` 文件（或统一 CLI 的子命令）
  - adb 调用一律用 `subprocess.run(["adb", ...])` 列表参数形式
  - 脚本文件统一用 write_to_file 写入完整内容后再执行

### 铁律 2：登录 / 验证码 / 风控 / 实名 / 支付 → 自动暂停，交还用户
- 检测到以下任一情况，**立即停止自动化**，截图 + 明确告知用户，等待人工完成后恢复：
  - 第三方 App 登录页（短信、一键登录、扫码登录）
  - 图形/滑块/短信验证码
  - 平台风控页（如"京东验证""安全验证"）
  - 实名认证、人脸识别
  - 任何支付/垫付/转账页面
- **不可绕过、不可尝试自动化**（合规红线 + 技术不可行）
- 恢复约定：AI 每次动作前先 `detect_state()`，识别到"人工已完成"则从断点继续

---

## 一、执行模式

### 🟡 熟手模式（默认）
- 仅保留 **1 个确认点**：最终提交前
- 其余环节（报名、跳转、填写）AI 直接执行
- 报名前做**红线自动检查**（见第四节），命中即中止并说明原因

### 🔴 终极模式（需显式开启）
- **零确认点**，AI 全程自动执行
- 提交前进行**口头自检播报**：朗读任务要求要点、已完成的证据、即将提交的内容，无异常即提交
- 强制补偿机制：
  - 红线检查升级为**双重**（提交前再查一遍）
  - 单任务金额 > 阈值（默认 ¥5）自动降级为熟手模式
  - 当日累计提交 > N 单（默认 30）自动停止并汇报
  - 全程操作日志留档，可回溯

---

## 二、主循环（提速版，目标 <60s/单）

```
① STATUS   zrb.py status    先识别环境与页面状态（连接/登录/当前界面）
② SCAN     zrb.py scan      扫描大厅 → 文本解析卡片（3s）→ 匹配模板/过滤
③ INSPECT  zrb.py inspect   读详情要求 → 红线检查 → 输出执行清单
④ PRECHECK 报名前预检：链接类任务先探测目标页存活（HTTP/关键字）
⑤ APPLY    zrb.py apply     报名（预检通过才报，不浪费名额）
⑥ EXECUTE  zrb.py exec      执行本体（开链接/看视频/截图）；QR 走极速通道
⑦ UPLOAD   zrb.py upload    证据截图入库 → 上传控件选图
⑧ SUBMIT   zrb.py submit    AI 生成反馈文案 → 熟手:确认后提交 / 终极:自检后提交
⑨ TRACK    台账 journal.jsonl：任务/状态/证据/时间；驳回提醒
⑩ CLEANUP  后台清理（任务完全结束后）：zrb.py cleanup
```

### 🧹 收尾清理（已实现 `zrb.py cleanup`）
| 步骤 | 内容 |
|---|---|
| 1 | `am force-stop` 结束任务用过的第三方 App（JD/得物/抖音/浏览器…；**保留**微信、支付宝、众人帮） |
| 2 | 多任务界面点「清理全部任务」（MIUI content-desc） |
| 3 | 重新拉起众人帮（`zrb_start`），回到大厅待命 |

**规则**：
- ⚠️ 清理**只能在任务完全结束后**执行（已提交 / 已取消）；任务中途清理会丢掉页面状态
- MIUI「清理全部任务」是**全局操作**：会关掉最近任务列表里的所有 App（包括微信/众人帮），不受保留名单控制
  → 想只杀指定 App、保留日常应用，用 `cleanup --no-recents`
- 被清掉众人帮后必须重新 `zrb_start`（业务状态存服务端，不丢数据，代价是 2~3s 冷启动）
- 默认清理名单来自 `app_map` 中已安装的第三方 App + 浏览器；可用 `--apps` 覆盖

**状态机**（每步先识别再动作，支持断点续跑）：
`未报名 → 已报名待执行 → 已执行待提交 → 已提交待审核 →（通过 | 驳回重做 | 放弃）`

---

## 三、效率规则（速度优先）

| # | 规则 | 说明 |
|---|---|---|
| 1 | **禁止盲等** | 所有 sleep 用 `wait_for(条件, 超时)` 轮询替代 |
| 2 | **定位表缓存** | 页面元素定位沉淀到 `ui_map.yaml`，探索一次复用永久，不反复 dump |
| 3 | **文本优先** | 能读控件树就不截图；截图仅用于取证 |
| 4 | **预授权** | 一次性 `pm grant`/`appops` 给全权限（众人帮/浏览器/相册/微信），消灭权限弹窗 |
| 5 | **通用弹窗处理器** | 每次操作前自动扫已知弹窗（同意/允许/知道了/暂不更新/关闭）并点掉 |
| 6 | **QR 极速通道** | 见下方专门流程，替代"微信取 URL"旧链路（省 ~60s） |
| 7 | **报名前预检** | 两种模式：HTTP 快检（静态页）/ **设备端渲染检**（活动页必用）。三态输出：存活/已死/不确定(需登录) |

### 预检的已知限制（实测结论）
- 活动页（如京东 pro.m.jd.com）是 JS 壳，HTTP 请求恒返回 200 → **快检会假阳性**
- 已死状态（"活动太火爆"）需**登录态 + 目标 App 内渲染**才能判定
- 预检输出三态：`存活` / `已死` / `不确定（需登录）`
- 策略：**已死 → 跳过不报名**；不确定 → 归属于"第三方助力类"，默认跳过，用户明确指定才做

### ⛔ 助力类任务的"结构性陷阱"（实测结论）
- 京东口令类任务写明「**报名后方可查看口令**」→ **报名前无法预检**（拿不到口令）
- 报名后才可能发现：`助力失败·好友任务已完成`（名额被抢完）→ 任务直接失败
- 且打开第三方 App 高频触发**风控验证页**（本试点命中 2 次）
- **规则**：
  1. 助力类任务**默认跳过**（收益低 0.17 元、依赖外部账号、风控高、需人工过验证）
  2. 用户明确指定才做，且必须：报名后**立即**执行验证 → 失败立即弃单（右上角举报/等超时）
  3. 每账号每日助力类尝试 ≤ 1~2 次（保护账号权重）

| 8 | **操作抖动** | 操作间隔 0.3–1.2s 随机（速度与安全平衡） |
| 9 | **依赖 App 预装** | 任务标题/要求命中目标 App（京东/得物/麦当劳/抖音…）→ **执行前先确认已安装**，未装则先装好再报名（避免执行中断+浪费时间）。见 `ui_map.yaml: app_map` |

### 预装机制（app_map）
- 维护「关键词 → 包名」映射（如 `京东 → com.jingdong.app.mall`、`得物 → com.shizhuang.duapp`、`麦当劳 → com.mcdonalds.app`）
- 执行流程改为：`inspect` 后 → `ensure_apps()` 检查依赖 → 缺则自动装（小米商店自动化已验证可行）→ 再 `apply`
- 装机属于一次性成本，装过一次永久复用

### QR 任务极速通道（已验证）
```
长按二维码 → 菜单选微信扫一扫 → 点"知道了"
   ↓ （App 自动把二维码原图落盘）
MediaStore 查询最新 /sdcard/Pictures/zrbdownload_*.png
   ↓
adb pull 原图 → 本地引擎解码（pyzbar / cv2.wechat / cv2.QRCodeDetector 轮试）
   ↓
得到 URL → 直接投递目标 App/浏览器
```
- 原图分辨率高（实测 2622px），三种引擎均可秒解
- ⚠️ 不走微信界面取 URL（会被"诱导页"拦截，且微信弹窗读不到控件树）

---

### ♻️ 善后流程：任务无法完成 → 取消报名（已实现 `zrb.py cancel`）
```
判定无法完成(外部条件不满足/风控/活动失效)
   ↓
待提交页：回首页 → 点悬浮球「待提交 N」→ 进入 ActivityUnsubmitAndWanted
   ↓
在「已报名」Tab 找到目标任务卡片 → 点 [取消报名]
   ↓
弹窗「您确定要取消报名此悬赏的名额么？」→ 点 [取消报名]
   ⚠️ 必须精确匹配 @text="取消报名"（contains 会命中"不取消报名"）
   ↓
校验成功：弹窗关闭 且 卡片按钮变为 [重新报名]
   ↓
记入台账（已取消报名 + 原因）
```
- 取消结果判定标准：**按钮变「重新报名」**（任务仍在列表中，只是退出报名状态）
- 已知坑：若只校验"标题消失"会误判（弹窗遮挡列表时标题读不到）

## 四、红线自动检查（内置拦截，不可绕过）
报名前 + 提交前各检查一次，任务要求/页面文本命中即跳过：
- 垫付、先付、押金、保证金、刷单、返利、验证码、银行卡、转账
- 下载非官方渠道 APK、要求提供密码/支付密码

---

## 五、任务模板（YAML）
```yaml
name: 模板名
match_keywords: []      # 标题命中即走快速通道
amount_min: 0
precheck_url: true      # 链接类任务报名前探测
steps:
  - open: 目标包名/网页
  - action: browse|watch|register|follow|play|fill
  - fill_from: ai
  - screenshot: [关键页面列表]
evidence_count: 2
submit_note_template: ""
```

## 六、任务类型与自动化可行性（试点结论）
| 类型 | 可行性 | 备注 |
|---|---|---|
| 浏览器开链接 + 截图 | ✅ 全自动 | 最优先刷 |
| 看视频/浏览类 | ✅ 全自动 | 注意页面停留时长要求 |
| QR 扫码类 | ✅（走极速通道） | 需预检活动页存活 |
| 第三方助力类 | ⚠️ 受限于外部 | 依赖用户账号 + 活动页状态，常需人工闸门 |
| 注册/实名/支付类 | ❌ 不做 | 铁律 2 |

## 七、风险控制常量
| 项 | 默认值 |
|---|---|
| 单任务金额降级阈值 | ¥5 |
| 每日自动提交上限 | 30 单 |
| 操作随机延迟 | 0.3–1.2s |
| 单任务超时 | 10 分钟 |

## 八、执行者约定
- 任务本体操作（含第三方 App）由 **AI 通过 uiautomator2 直接执行**
- 暂停交还用户的节点：严格按**铁律 2** 执行
- App 崩溃、控件找不到 → 重试 2 次后暂停汇报
